:root {
  --bg-color: #0F172A;
  --card-bg: #1E293B;
  --primary: #10B981;
  --primary-dark: #065F46;
  --accent: #F59E0B;
  --danger: #EF4444;
  --text-main: #F8FAFC;
  --text-sub: #94A3B8;
  --radius: 20px;
}

/* 🚫 核心：消除瀏覽器網頁特徵 (長按選取、雙擊放大、藍色高亮) */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  -webkit-touch-callout: none;
  -webkit-user-select: none;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}

input { user-select: text; -webkit-user-select: text; }

body {
  background-color: #020617;
  color: var(--text-main);
  display: flex;
  justify-content: center;
  height: 100vh;
  overflow: hidden;
}

.app-viewport {
  width: 100%;
  max-width: 430px;
  height: 100%;
  background: var(--bg-color);
  display: flex;
  flex-direction: column;
  position: relative;
}

/*  Header */
.app-header {
  padding: 12px 16px;
  background: var(--card-bg);
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #334155;
}
.user-info { display: flex; align-items: center; gap: 10px; }
.user-avatar { font-size: 26px; }
.user-name { font-weight: bold; font-size: 14px; }
.user-badges { display: flex; gap: 6px; margin-top: 2px; }
.badge { background: #334155; padding: 2px 8px; border-radius: 10px; font-size: 11px; font-weight: bold; }
.badge.streak { color: var(--accent); }
.badge.xp { color: var(--primary); }
.icon-btn { background: none; border: none; font-size: 18px; cursor: pointer; }

/* Language Selector Bar */
.lang-bar {
  padding: 8px 16px;
  background: #182234;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
select { background: #334155; color: white; border: none; padding: 6px 12px; border-radius: 10px; font-weight: bold; }
.sync-status { font-size: 11px; color: var(--text-sub); }

/* Main Area */
.app-main { flex: 1; overflow-y: auto; padding: 15px; padding-bottom: 80px; }
.tab-page.hidden { display: none; }

/* Cards & Buttons */
.card-box {
  background: var(--card-bg);
  padding: 18px;
  border-radius: var(--radius);
  border: 1px solid #334155;
  position: relative;
}
.btn {
  width: 100%;
  padding: 12px;
  border-radius: 12px;
  border: none;
  font-weight: bold;
  font-size: 14px;
  cursor: pointer;
}
.btn.primary { background: var(--primary); color: #022C22; }
.btn.secondary { background: #334155; color: white; }
.btn.danger { background: var(--danger); color: white; }

/* SRS Flashcard 翻卡效果 */
.srs-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; }
.srs-count { font-size: 12px; color: var(--text-sub); }

.flashcard-container { perspective: 1000px; height: 260px; margin-bottom: 15px; }
.flashcard {
  width: 100%; height: 100%;
  position: relative;
  transform-style: preserve-3d;
  transition: transform 0.6s;
  cursor: pointer;
}
.flashcard.flipped { transform: rotateY(180deg); }
.card-face {
  position: absolute; width: 100%; height: 100%;
  backface-visibility: hidden;
  border-radius: var(--radius);
  background: var(--card-bg);
  border: 1px solid #334155;
  padding: 20px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  text-align: center;
}
.card-back { transform: rotateY(180deg); background: #1E293B; }

.word-cat { font-size: 11px; color: var(--accent); background: rgba(245,158,11,0.1); padding: 4px 10px; border-radius: 8px; margin-bottom: 8px; }
.word-text { font-size: 32px; font-weight: bold; margin-bottom: 4px; }
.word-reading { font-size: 14px; color: var(--text-sub); }
.audio-btn { background: #334155; border: none; color: white; padding: 6px 14px; border-radius: 20px; margin-top: 10px; cursor: pointer; }
.flip-hint { font-size: 10px; color: #64748B; margin-top: 15px; }

.word-meaning { font-size: 24px; color: var(--primary); margin-bottom: 15px; }
.example-box { background: #0F172A; padding: 12px; border-radius: 12px; width: 100%; text-align: left; }
.example-src { font-size: 13px; color: #E2E8F0; }
.example-tgt { font-size: 12px; color: var(--text-sub); margin-top: 4px; }

.srs-controls { display: flex; gap: 8px; }
.srs-btn { flex: 1; padding: 10px 4px; border: none; border-radius: 10px; font-size: 11px; font-weight: bold; color: white; cursor: pointer; }
.srs-btn.hard { background: #991B1B; }
.srs-btn.good { background: #854D0E; }
.srs-btn.easy { background: #065F46; }

/* Match Game Grid */
.match-grid-container { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
.m-card { background: var(--card-bg); border: 1px solid #334155; padding: 16px; border-radius: 12px; text-align: center; font-weight: bold; cursor: pointer; }
.m-card.selected { border-color: var(--accent); background: rgba(245,158,11,0.2); }
.m-card.matched { visibility: hidden; }

/* Map Nodes */
.nodes-path { display: flex; flex-direction: column; align-items: center; gap: 20px; padding: 20px 0; }
.node-btn { width: 70px; height: 70px; border-radius: 50%; border: none; font-size: 22px; font-weight: bold; background: #334155; color: white; box-shadow: 0 6px 0 #1E293B; }
.node-btn.current { background: var(--accent); box-shadow: 0 6px 0 #D97706; }
.node-btn.done { background: var(--primary); box-shadow: 0 6px 0 #059669; }

/* Bottom Nav */
.app-nav {
  position: absolute; bottom: 0; left: 0; right: 0; height: 65px;
  background: rgba(30, 41, 59, 0.95); backdrop-filter: blur(10px);
  display: flex; justify-content: space-around; align-items: center;
  border-top: 1px solid #334155;
}
.nav-btn { background: none; border: none; display: flex; flex-direction: column; align-items: center; color: var(--text-sub); flex: 1; }
.nav-btn.active { color: var(--primary); font-weight: bold; }
.nav-icon { font-size: 20px; }
.nav-label { font-size: 11px; margin-top: 2px; }

/* Modal */
.auth-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.8); backdrop-filter: blur(8px); display: flex; justify-content: center; align-items: center; z-index: 9999; }
.auth-overlay.hidden { display: none; }
.auth-card { background: var(--card-bg); width: 85%; max-width: 340px; padding: 25px; border-radius: 24px; text-align: center; }
.auth-body input { width: 100%; padding: 12px; margin-bottom: 10px; border-radius: 12px; border: 1px solid #334155; background: #0F172A; color: white; outline: none; }
.divider { margin: 15px 0; position: relative; font-size: 12px; color: var(--text-sub); }
.mt-5 { margin-top: 5px; } .mt-10 { margin-top: 10px; } .mt-15 { margin-top: 15px; }
.text-center { text-align: center; } .highlight { color: var(--primary); font-weight: bold; }
.stamp { position: absolute; top: 12px; right: 12px; border: 2px solid var(--danger); color: var(--danger); padding: 2px 6px; font-size: 10px; font-weight: bold; border-radius: 4px; transform: rotate(10deg); }